import React, { ReactElement } from 'react';

interface Props {
  
}

export default function ProductsPage({}: Props): ReactElement {
  return (
    <div>
        <h1>Products page</h1>
    </div>
  )
}
